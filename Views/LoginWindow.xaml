<Window x:Class="Deno.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Deno.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="UsernameBox" Grid.Row="0" Margin="0 10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0 10"/>

        <Button Grid.Row="2" Content="Login" Width="100" Height="30" 
                HorizontalAlignment="Right" Click="Login_Click"/>
    </Grid>
</Window>
